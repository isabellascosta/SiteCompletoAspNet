	var_containerMenu = $('[data-container="menu"]');

	$(window).scroll (funcion(){
		if{$(this).scrollTop()>70{
			_containerMenu.addclass('bloco-fixo'{
		}else{
			_container.fadeOut(0);
		}

	_container$.click(functions(){
	$("hmtl.body").animate({scrollTop: 0}, 800);
	return false;
	});